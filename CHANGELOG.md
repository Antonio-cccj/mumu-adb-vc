# 更新公告 / Changelog

> 王者荣耀农场一键务农自动化（MuMu + ADB + OpenCV）

---

## v1.1.1 — 2026-06-17

### 🐛 修复：「明天」前缀漏识别（明天HH:MM 被读成今天）

- **问题**：实测「明天14:10成熟」被读成今天「14:10」。原因是「明天」判定以**跨分辨率/渲染极不稳定的模板匹配（CCOEFF 阈值 0.78）为唯一依据**，真实卡片文字偏亮、`inRange` 只抓到汉字暗边缘碎片，模板分数远低于阈值直接失配；继而 32h 作物剩余时间从 ~27h 误算成 ~3h，调度严重错误。
- **修复**（`maturity_reader.py`）：按需求「**先判明天前缀、再读 HH:MM**」重构为**几何判据为主、模板匹配为辅**：
  - 在「首位数字左侧、与数字同基线」范围内寻找**汉字字身**——高度≈数字、且至少一个分量**近正方形**（宽 ≥ 0.70×字高，这是汉字与"数字状窄条/边框竖线"的关键区别）。
  - 要求前缀**横向跨度 ≥ 1.20×字高**（「明天」实测≈2.4×）且**紧邻时间数字**（缝隙 ≤ 1.10×字高），排除卡片左侧美术/图标。
  - **笔画密度** 落在 `[0.20, 0.62]`：剔除实心色块/实心噪声竖条（密度≈1.0）与空白。
  - 分量归属改用「中心在首位数字左侧」，正确纳入紧贴数字的「天」字、自然排除首位数字本身。
  - 移除以模板匹配为主判据的旧 `_has_tomorrow_prefix` 及相关死代码。
- **回归测试**：新增 `test_read_maturity_time_reads_tomorrow_1410_regression`，并用真实截图裁出的「明天14:10」验证判定为 `明天14:10 / day_offset=1`。全套测试 115 项通过。

---

## v1.1.0 — 2026-06-17

本次聚焦修复「成熟时间识别」与「定时调度」两个核心算法 Bug，并新增「自然成熟防偷菜」机制。

### 🐛 修复：成熟时间 OCR 识别出错

- **问题**：旧的二值化只按面积/高度做弱过滤，卡片边框、水分蓝条、右下「?」按钮、以及「成熟」两个汉字都会被当成数字候选并被误判（置信度甚至高达 0.8+），导致 `HH:MM` 偶发读错、「明天」前缀误判。
- **修复**（`maturity_reader.py`）：
  - 几何过滤——按「宽高比 + 相对中位高度」剔除边框/色块/按钮/汉字等噪声（数字瘦高、汉字近正方形）。
  - 行聚类——四位数字共享同一基线，只取「数字所在行」，彻底排除上下行干扰。
  - 一致性加权——用高度接近、间距均匀（中间因冒号略宽）给真实时间加分，压制偏旁拼出的「伪时间」。
  - 「明天」前缀只在数字行竖直范围内、首位数字左侧用专用模板匹配，定位更稳。
  - 铲子锚点阈值 `0.70 → 0.64`（真实截图实测分数 0.664，原阈值会让锚点失效）。
- **效果**：真实截图 `full_1 / full_2` 数字行精确提取为 `1 3 0 4`，零噪声读出 `13:04`，明天前缀判定正确。

### 🐛 修复：定时调度把启动排到成熟时间之后

- **问题**：当剩余成熟时间很小（如 32h 作物剩 15 分钟）时，最后一浇间隔被「最低浇水间隔 T/30」无脑抬高（64 分钟），导致下次定时启动被排到了作物成熟之后。
- **修复**（`run_scheduler.py`）：
  - 去掉会把启动推到成熟之后的最低间隔下限。
  - 新增 `natural_harvest` 阶段：当剩余 < T/24（连最低浇水间隔都赶不上自然成熟）时，按真实剩余时间安排收获，**下次启动永不晚于成熟时刻**。
- 不论何时启动脚本，都会以识别到的剩余时间为输入，将总耗时收敛到理论最快 `0.8R`（1.25 倍速极限），即全局最优解。

### ✨ 新增：自然成熟防偷菜（提前到位等待）

- **场景**：作物自然成熟（非浇水触发）时，若脚本在成熟后才启动+导航，成熟未收的作物可能被偷。
- **机制**：
  - 仅当最优解判定为 `natural_harvest` 时，启动时刻自动提前 `5` 分钟（`DEFAULT_HARVEST_LEAD`）。
  - 新增任务节点 `wait_for_harvest`：脚本先导航到「一键务农」按钮前，**到点再点击收获**；其余情况（浇水 / 终浇即收）直接放行，不等待。
- 报告示例：`08:43 提前到按钮前等待，08:48 自然成熟收获`。

### 🧹 项目整理

- 清理 `debug/` 运行截图缓存约 **1.4 GB**（保留 `template_sources/`、`failure_snapshots/`）。
- 真实成熟卡样本与根目录散落参考图统一收纳到 `debug/template_sources/`（`maturity_samples/`、`manual_refs/`）。
- 删除空目录 `dist_unlocked/`、中间产物 `build/`、`.pytest_cache/` 与各 `__pycache__/`。

### ✅ 测试

- 全量单测 **114 项通过**；新增覆盖：自然成熟收获、提前量边界、「下次启动永不晚于成熟」不变式、含噪声的真实风格 OCR、`wait_for_harvest` 等待/放行。

### ⚠️ 升级说明

- 以上为源码改动。使用打包版 `release/MuMuADBVC/MuMuADBVC.exe` 的用户需重新构建覆盖：

```powershell
python build_exe.py
```

- 如模拟器启动+进游戏+导航经常超过 5 分钟，可调大 `run_scheduler.py` 中的 `DEFAULT_HARVEST_LEAD`。
